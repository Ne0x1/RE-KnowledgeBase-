بص يا سيدي، لو أنت شغال في الـ Reverse Engineering (RE) أو الـ Malware Analysis، دالة CreateFile دي هي "البوابة الملكية" اللي تقريبًا أي برنامج خبيث (Malware) بيعدي عليها.

الدالة دي تبع الـ Windows API (موجودة في مكتبة kernel32.dll)، ومن اسمها كده تفتكر أنها بتعمل ملفات بس، صح؟ لأ، الدالة دي صايعة جدًا واسمها مخادع!

إليك الشرح بالبلدي كده وعامّية مبسطة:
الدالة دي بتعمل إيه بالظبط؟

الملوير مش بس بيستخدمها عشان يكريت (ينشئ) ملف جديد، لأ، هو بيستخدمها كمان عشان يفتح ملف موجود بالفعل، أو عشان يتعامل مع حاجات تانية خالص في الويندوز زي:

    المجلدات (Directories).

    الفلاشات والهاردات (Physical Drives / Volumes) عشان يلعب في الـ MBR أو يشفر الداتا (زي الفدية).

    مخارج السيريال والـ USB (Ports).

    الـ Named Pipes عشان يكلم بروسيس تانية.

    💡 من الآخر: الدالة دي بتقول للوندوز: "لو سمحت افتح لي خط مع الحاجه الفلانية دي عشان عايز أتعامل معاها"، والوندوز بيرد عليها بـ Handle (زي مفتاح أو ريموت كنترول) يخلي الملوير يقدر يقرا أو يكتب بعد كده.

الملوير بيستخدمها في إيه؟ (منظور الـ Malware Analysis)

وأنت بتحلل العينة، أول ما تلمح CreateFile في الـ API Monitor أو وأنت بتبص في الـ Disassembler (زي IDA Pro أو Ghidra)، لازم عينك وسط رأسك، لأنها بتكشف لك نوايا الملوير:

    Drop & Execute (رمي وتشغيل ملفات):
    تلاقي الملوير فاتح CreateFile عشان يعمل ملف .exe أو .dll جديد في الفولدرات المؤقتة (Temp) ويرمي جواه الكود الخبيث بتاعه.

    برامج الفدية (Ransomware):
    دي دالتهم المفضلة. بيلفوا على كل ملفات الجهاز، ويستخدموا CreateFile عشان يفتحوا الملف، يقروا الداتا اللي فيه، يشفروها، وبعدين يمسحوا الأصلي.

    سرقة البيانات (Data Exfiltration / Keylogging):
    الملوير ممكن يفتح ملف لوج (.txt أو .dat) عشان يخزن فيه ضغطات الكيبورد اللي أنت بتكتبها، أو يفتح ملفات حساسة عندك عشان يقراها ويبعتها للهكر.

    تدمير النظام (Wipers):
    بيستخدموها عشان يفتحوا الهارد نفسه كـ \\.\PhysicalDrive0 ويفرمتوا الـ MBR (منطقة الإقلاع) عشان الجهاز ما يقومش تاني.

الباراميترز المهمة اللي لازم تركز معاها (وأنت بتعمل RE)

الدالة دي بتاخد كذا باراميتر، وأنت في الـ Debugger (زي x64dbg) لازم تبص على قيم الباراميترز دي عشان تفهم الملوير ناوي على إيه:

    lpFileName (اسم الملف): دا أهم واحد. هيوريك الملوير رايح يلعب في أنهي ملف أو مسار (مثلاً رايح لـ C:\Windows\System32 ولا رايح لـ Temp؟).

    dwDesiredAccess (نوع الصلاحية):

        لو لقيتها GENERIC_READ (0x80000000): الملوير جاي يقرا بس (بيسرق داتا أو بيقرا إعدادات).

        لو لقيتها GENERIC_WRITE (0x40000000): الملوير جاي يكتب (بيعدل ملف أو بيرمي ملوير جديد).

    dwCreationDisposition (هيعمل إيه بالملف):

        CREATE_ALWAYS (2) أو CREATE_NEW (1): معناه لازم هينشئ ملف جديد (Drop لملف جديد).

        OPEN_EXISTING (3): بيفتح ملف موجود أصلاً (عشان يشفر أو يسرق).

الخلاصة للمحلل:

لما تلاقي CreateFile في الملوير، حط Breakpoint عليها فورا. أول ما البرنامج يقف عندها، بص على الـ Stack وشوف اسم الملف اللي هيتفتح ونوع الصلاحية. دي هتكون أول خيط يخليك تمسك الملوير بيعمل إيه في الـ File System بتاع الضحية!
